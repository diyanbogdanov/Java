
package onetoten;

public class OneToTen {

    public static void main(String[] args) {
        int zero;
        int one;
        int two;
        int three;
        int four;
        int five;
        int six;
        int seven;
        int eight;
        int nine;
        int ten;
        
        int sqrtZero;
        int sqrtOne;
        int sqrtTwo;
        int sqrtThree;
        int sqrtFour;
        int sqrtFive;
        int sqrtSix;
        int sqrtSeven;
        int sqrtEight;
        int sqrtNine;
        int sqrtTen;
        
        int cubeZero;
        int cubeOne;
        int cubeTwo;
        int cubeThree;
        int cubeFour;
        int cubeFive;
        int cubeSix;
        int cubeSeven;
        int cubeEight;
        int cubeNine;
        int cubeTen;
        
        zero = 0;
        one = 1;
        two = 2;
        three = 3;
        four = 4;
        five = 5;
        six = 6;
        seven = 7;
        eight = 8;
        nine = 9;
        ten = 10;
        
        sqrtZero = (int)Math.pow(zero, 2);
        sqrtOne = (int)Math.pow(one, 2);
        sqrtTwo = (int)Math.pow(two, 2);
        sqrtThree = (int)Math.pow(three, 2);
        sqrtFour = (int)Math.pow(four, 2);
        sqrtFive = (int)Math.pow(five, 2);
        sqrtSix = (int)Math.pow(six, 2);
        sqrtSeven = (int)Math.pow(seven, 2);
        sqrtEight = (int)Math.pow(eight, 2);
        sqrtNine = (int)Math.pow(nine, 2);
        sqrtTen = (int)Math.pow(ten, 2);
        
        cubeZero = (int)Math.pow(zero, 3);
        cubeOne = (int)Math.pow(one, 3);
        cubeTwo = (int)Math.pow(two, 3);
        cubeThree = (int)Math.pow(three, 3);
        cubeFour = (int)Math.pow(four, 3);
        cubeFive = (int)Math.pow(five, 3);
        cubeSix = (int)Math.pow(six, 3);
        cubeSeven = (int)Math.pow(seven, 3);
        cubeEight = (int)Math.pow(eight, 3);
        cubeNine = (int)Math.pow(nine, 3);
        cubeTen = (int)Math.pow(ten, 3);
        
        System.out.printf("Number\tsquare\tcube\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n%d\t\t%d\t\t%d\n",
                zero, sqrtZero, cubeZero,
                one, sqrtOne,cubeOne,
                two, sqrtTwo, cubeTwo,
                three, sqrtThree, cubeThree,
                four, sqrtFour, cubeFour,
                five, sqrtFive, cubeFive,
                six, sqrtSix, cubeSix,
                seven, sqrtSeven, cubeSeven,
                eight, sqrtEight, cubeEight,
                nine, sqrtNine, cubeNine,
                ten, sqrtTen, cubeTen);
        
    }
    
}
